import React from "react";

const FormLabel = ({ value }) => {
  return (
    <span className="text-sm uppercase font-medium tracking-wide text-primary_text_1">
      {value}
    </span>
  );
};

export default FormLabel;
